import { {{class}} } from './{{name}}';

describe('CustomElement', () => {
  let element: {{class}};

  beforeEach(() => {
    element = new  {{class}}();
  });

  it('should create successfully', () => {
    expect(element).toBeTruthy();
  });
});