import * as React from 'react';

import * as styles from './{{name}}.{{style}}';

export const {{name}} = ({ children }) => (
  <div>
    <p>New Component {{ name }}</p>
    {children}
  </div>
);

export default {{name}};
