import { cleanup, render } from '@testing-library/react';
import * as React from 'react';

import {{name}} from './{{name}}';

describe('<{{name}} />', () => {
  it('renders without crashing', () => {
    const cmp = render(<{{name}} />);
     
    expect(cmp).toBeDefined();
    cleanup();
  });
});
